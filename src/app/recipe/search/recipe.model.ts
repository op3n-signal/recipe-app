export class Recipe {
    constructor(public title: string, public baseUri: string, public image: string, public id: number, public readyInMinutes: number, public servings: string, public sourceUrl: string){};
}